<h1>Ingredients</h1>


<p>Sorted <%= @sorting_mode.to_s %></p>

<% if @ingredients %>
  <ul>
  <% @ingredients.each do |ingr| %>
    <% i = ingr[:i] %>
    
    <li> <%= "#{i.nice_text} (#{i.recipe.title} para #{i.recipe.yields})" %></li>
  <% end %>
  </ul>
<% end %>
